interface:
  display_name: "Post Pilot"
  short_description: "Repo-specific patterns and workflows for post-pilot"
  default_prompt: "Use the post-pilot repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true